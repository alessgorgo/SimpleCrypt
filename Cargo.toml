[workspace]
members = ["simplecrypt-core", "simplecrypt-cli", "simplecrypt-gui"]
resolver = "2"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
opt-level = 3

[profile.release.package."*"]
opt-level = 3
